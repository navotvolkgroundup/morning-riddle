# Byte-identical to m5stack/M5PaperColor-UserDemo's table.
#
# This started as a "tidier" layout -- app at 0x20000, no FAT partition, since
# config comes from the microSD card and the network. It built, it flashed,
# every hash verified, and the application never started, on a board that runs
# M5Stack's own firmware perfectly well. Novel layouts are not worth debugging
# against a working reference; the storage partition is simply left unused.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x9F0000,
storage,  data, fat,     0xA00000, 0x600000,
